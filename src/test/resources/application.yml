#SPRING CONFIGURATION

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:paymentDevDb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
